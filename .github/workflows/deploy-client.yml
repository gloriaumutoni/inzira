name: Deploy Client

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Skip — Vercel not configured yet
        run: echo "Vercel deployment will be configured when frontend is ready"
