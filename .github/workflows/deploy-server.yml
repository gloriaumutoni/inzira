name: Deploy Server

on:

  push:

    branches: [main]

jobs:

  deploy:

    name: Deploy to Render

    runs-on: ubuntu-latest

    steps:

      - name: Checkout code

        uses: actions/checkout@v4

      - name: Trigger Render deploy

        run: |

          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"name: Deploy Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Trigger Render deploy
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Render
        run: |
          curl -s -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
